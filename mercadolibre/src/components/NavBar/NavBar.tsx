import React, { FunctionComponent, ReactElement } from "react";

interface navBarProps {
    onType: (searchInput: string) => void
}

const NavBar: FunctionComponent<navBarProps> = ({ onType }): ReactElement => {

    return (
        <div />
    )
}

export default NavBar